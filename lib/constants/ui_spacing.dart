/// UI spacing constants for consistent layout across signup/onboarding flows.
///
/// Use [kSignupFlowButtonBottomSpacing] for the bottom padding of primary
/// action buttons (AppButton) to avoid overlap with system navigation and
/// keep spacing consistent across all signup pages (edu and non-edu).

/// Bottom spacing for primary buttons in signup and onboarding flows.
/// Applied via AppButton's bottomSpacing to maintain 60px from button
/// to the bottom of the content area / system UI.
const double kSignupFlowButtonBottomSpacing = 60;
